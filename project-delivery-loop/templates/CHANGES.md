# CHANGES — <PROJECT NAME>
_Append-only, newest first. Corrections reference the earlier change ID; do not rewrite evidence._
_Rotate at 50 entries / 1500 lines; use the installed skill's ledger_rotate.py with --dry-run first._
_The initializer omits example entries. Create a record only for actual work._

## C-0001 — <title>
- When: <ts>
- Author: <actual agent or human identity>
- Task: T-0001
- Commit: <real SHA or UNCOMMITTED — reason; never invent a SHA>
- What: <actual change; distinguish implementation from verification>
- Why: <need or defect reference>
- Files: <paths actually changed>
- Status: PARTIAL
- Delivery: NOT_DEPLOYED
- Verification:
  - Context: <working directory, environment, revision/working-tree state, actual timestamp>
  - `<command actually run>` -> <PASS / FAIL / BLOCKED / NOT_RUN> (exit <code if executed>; <observed result>)
  - Manual: <actual steps or not performed> -> <outcome and observed behavior, not a planned check>
  - Not covered: <specific limitation, owner/next action; none within scope only if supported>
- Follow-ups: <task IDs or none>
